# The service's runtime. Pinned, because a key-management service that resolves
# a different crypto library on a different day is not one you can attest to.
#
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
cryptography==50.0.0
orjson==3.12.0
structlog==26.1.0
