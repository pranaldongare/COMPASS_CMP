# DKMS — encryption of personal data

Everything about the key service and what it protects, in five documents.
Personal data is **encrypted before it reaches the database** and
**decrypted only in the frontend layer**: the platform API stores and serves
ciphertext, and a portal opens a value at the moment a person reads it.

| Read this | For |
|---|---|
| [PII tables and fields](pii-tables-and-fields.md) | What the platform holds about people, table by table and column by column: which are sealed and as what type, which carry a `*_hash` or `*_ngrams` beside them, which are plaintext and why |
| [The backend](backend-api.md) | Where encryption happens, the key service's own contract and what another service must provide, the 159 endpoints that carry personal data, and the places the backend decrypts |
| [The frontend layer](frontend-layer.md) | How the two portals open every response, how a sealed name is still searched for, which APIs that covers, the four files that do it, the one setting, and what breaks it |
| [Adding a personal field](adding-a-personal-field.md) | The checklist: everything that changes together when a column joins the list, and which steps nothing checks |
| [The implementation plan](implementation-plan.md) | What was built and in what order, what each phase proved, what a new environment costs, and how each failure shows |

Start with the plan if you are new to it, the field list if you are
answering "what do we hold about a person", the checklist if you are adding
a field, the backend if you are changing how one is written or read, the
frontend if a page is showing `SE::…`.

Related, elsewhere in the tree:

- [Personal data](../domain/personal-data.md) — the narrative inventory, every store and not only the database
- [PII fields and endpoints](../domain/pii-fields-and-endpoints.md) — the generated list, regenerated by `docs/tools/pii-fields-and-endpoints.py`
- [ADR 0016](../decisions/0016-personal-data-sealed-by-a-separate-key-service.md) — why personal data is sealed by a separate key service
- [ADR 0017](../decisions/0017-lookup-by-keyed-hash-and-name-ngrams.md) — why rows are found by a keyed hash, and three names by hashed runs
- [ADR 0015](../decisions/0015-nothing-erasable-in-a-trail-nobody-can-erase.md) — why nothing erasable goes into the audit trail
- [Encryption at rest](../security/encryption-at-rest.md) — what the sealing protects against, and what it does not
- [Runbook](../operations/runbook.md) — no message of any kind is sent, and the request said one was
- [backend/dkms/README.md](../../backend/dkms/README.md) — how to run the service
