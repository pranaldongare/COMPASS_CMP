/**
 * The query layer: keys, shared option types, and the generic write hook.
 */

export { keys, prefixes } from "@/lib/query/keys";
export { useApiMutation } from "@/lib/query/options";
export type { ListFilters, Options, Result } from "@/lib/query/options";
